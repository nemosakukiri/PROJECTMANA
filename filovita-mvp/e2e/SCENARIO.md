# E2E シナリオ：予約票の撮影から、後日その情報で支援を受けるまで

本番を想定した「生活の中で最後まで目的を達成できるか」を検証するための、
Filovitaの一連の利用フロー。単体機能ではなく、一人の利用者が実際に辿る
時系列で記述する。

各ステップに実装状況を明記する。`full-life-journey.spec.mjs` は
**実装済みの部分だけ**を、本物のUI操作（クリック・入力・リロード）を
通して連続実行する。実装されていない部分はテストでは飛ばし、その旨を
このファイルとテスト実行時のログの両方に明記する。

| # | ステップ | 状態 |
|---|---|---|
| 1 | 予約票・チラシを撮影する | ❌ 未実装（InputScreenに撮影UIが存在しない） |
| 2 | OCR・AIが写真から日時・場所・電話番号などを抽出する | ❌ 未実装（`fakeGenerateDraft.js`はテキストのみを扱う。OCRパイプラインは存在しない） |
| 3 | 音声/文字入力からAIが下書きを作る | ✅ 実装済み（フェイクAI、`fakeGenerateDraft.js`） |
| 3.5 | 実際に声で入力する | ✅ 実装済み（Web Speech API、`InputScreen.jsx`）。このテストではマイクハードウェアが無い実行環境のため`SpeechRecognition`をモックし、認識結果を受け取った後のUI・データの流れは本物のまま検証する |
| 4 | 内容を確認してEventとして確定する | ✅ 実装済み（`ConfirmScreen.jsx` → `App.jsx`の`handleConfirm`） |
| 5 | Eventがカレンダーに登録される | ✅ 実装済み（Filovita内部のカレンダー）。❌ Google Calendarとの双方向連携は未実装（次回セッションの最優先事項） |
| 6 | AIの長期記憶に反映される（タグの道具箱に参照が育つ） | ⚠️ 部分実装：タグを自動生成するAIはまだ無いが、タグが付いた後の「道具箱に参照を登録する」仕組みは本物（`TagToolboxScreen.jsx`） |
| 7 | 重要な一文にマーカーを引く | ✅ 実装済み（`MarkableText.jsx`） |
| 8 | 後日、その情報を使って支援を受ける（電話する・地図を開く・マーカーされた記録を横断的に見る） | ✅ 実装済み。テストでは実際に`page.reload()`を挟み、localStorageからの復元を経てから検証することで「後日」を模擬する |

## 今回のテストが検証すること

1. 音声入力（モック）→ AI下書き → 確認 → Event確定、が実際にカレンダーへ反映される
2. 所感を書くと`所感`タグが自動で付き、タグの道具箱を開いて参照（電話番号）を登録できる
3. 登録した参照から「電話する」ボタンが正しい`tel:`リンクを持つ
4. 結論文にマーカーを引ける
5. **ページを実際にリロードしてもすべて残っている**（Event・タグ・道具箱の参照・マーカー）
6. リロード後、タグの道具箱を開くと「マーカーされた記録」欄に、後で見つけたマーカーが日付付きで一覧表示される
7. 他のテーマ（このテスト対象外のテーマ）を壊していないかは、このテストの範囲外。回帰確認は`e2e/README.md`にある通り既存の手動スクリーンショット検証で別途行う

## 未実装ギャップ（次に埋めるべきもの、優先順）

1. Google Calendar実連携（すでにWORKLOGに記録済み、次回セッション最優先）
2. 予約票・チラシの撮影 → OCR → AI抽出のパイプライン
3. タグの自動生成（現状はスタンプの手動選択と「所感」のみ。写真からのAI抽出ができて初めて`#病院`のような意味のあるタグが自動で付く）
