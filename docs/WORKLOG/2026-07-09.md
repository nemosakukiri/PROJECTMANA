# WORKLOG 2026-07-09（PROJECT MANA）

## 前回からの申し送り
- 収集ソース追加（論考のシノドス偏重是正＋社説の主要紙まるごと）は本番GAS反映済み。反映は次回collectNewsから（→ユーザーが「シノドスしか入ってない」と確認したのは収集前のため。回れば増える見込み）。
- 宿題：戦争の村「準備中」3軒の中身／観測DBの表示一貫性／絵本の未使用Supabase連携外し（再発防止）／絵本ロゴ完成時のPWAアイコン差し替え。

---

## 今日やったこと

### 「MANAの暮らし」を新設（運営の随筆コーナー）
- 目的：ホームの木の巣箱に一つ足して「MANAの暮らし」とし、運営（多藝麻奈）が**生活に基づいた文章を時々置く**場所にする。
- 実装（コミット：MANAの暮らし追加）：
  - `app.js` ホームの巣箱配列に `{ label:'MANAの暮らし', sub:'よみもの', action:renderKurashiPage(), x:bx+20,y:352,w:104,h:52 }` を追加（民主主義の言葉の隣に仮配置）。
  - `app.js` に `renderKurashiPage()` 追加：`/mana_kurashi.json` を読み、`#page-kurashi` にタイトル/著者/日付/節見出し/段落で描画。戻るボタンでホームへ。
  - `index.html` に `<div class="page" id="page-kurashi"></div>` 追加。
  - `style.css` に `.kurashi-*` スタイル追加（読みやすい随筆レイアウト・レスポンシブ）。
  - `mana_kurashi.json`（新規・静的）に初回エッセイを格納。
- **初回エッセイ**：「支援で私が受けた傷 — 極めて主観的な記録」（多藝麻奈・2026-07-08）。傷1〜傷4。
  - ユーザー提供PDFを**言葉は改変せず**掲載。OCR誤りのみ修正（玄関の「伴」→「鍵」、「あうべて」→「すべて」）。

### 今後の運用（重要）
- 文章の追加は **`mana_kurashi.json` に要素を足すだけ**（配列の1essay = {id,title,subtitle,author,date,sections:[{heading,paragraphs:[]}]}）。
- ユーザーが書いた文章を渡してくれたら、こちらがJSONに追記してpush。GitHub→Vercel自動デプロイ。
- 静的ファイルなので反映確認はハードリロード。将来、ユーザーが自分で書ける仕組み（シート等）に発展も可。

---

## 未確認・次にやること
- 巣箱の配置（x:bx+20,y:352）が木の中で他と重ならず見えるか、実機で要確認（重なれば座標調整）。
- 収集した論考・社説が実際にDB/各家に増えたか（collectNews後）を確認。
- 継続：戦争の村の中身／観測DB表示一貫性／絵本側の宿題。
