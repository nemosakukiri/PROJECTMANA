# 作業日誌 2026-06-24

## 1. 本日の作業内容

- Step1：観測メタデータ列の追加（gas_main.gs）
  - COL定数に6列追加：title_normalized / source_domain / dedup_hash / law_refs_raw / institution_refs_raw / tag_source
  - LAW_NAMES定数（12法令名）追加
  - INSTITUTION_NAMES定数（31機関名）追加
  - normalizeTitle() / extractDomain() / computeDedupHash() / extractLawRefs() / extractInstitutionRefs() 追加
  - buildRowLayerA() に6列のset()追記
  - getOrCreateFullLogSheet() / getOrCreatePublicSheet() のヘッダーに6列追加
  - migrateAddObservationColumns() 追加
  - backfillObservationFields(limit) 追加
- GitHubへのpush（claude/busy-pascal-0o7ane ブランチ）
- GitHub App（Claude）のInstalled GitHub Appsへのインストール

## 2. 変更したファイル

- `gas_main.gs`（コミット: bb5bd12）
- `docs/WORKLOG/2026-06-24.md`（本ファイル・新規）

## 3. 成功したこと

- gas_main.gs の実装完了
- GitHub App（Claude）を https://github.com/apps/claude からインストール
- `git push -u origin claude/busy-pascal-0o7ane` 成功
- GitHubリポジトリへの書き込み権限が有効になった
- backfillObservationFields 完了：kansokuDB 431件・観測DB（全件ログ）584件すべてにメタデータ付与

## 4. 失敗・混乱したこと（セッション2追記含む）

- 前セッションで「GASコードの直接編集✅」と申し送りしたが、本セッションで再現できなかった
- Google Drive MCPにはGASファイルの更新ツールが存在しないことが判明
- GASへの反映（コード貼り付け）が未完了のまま時間を使った
- Claude Code デスクトップアプリが起動不全（真っ黒）で使用不可だった
- GitHub App のインストール方法を何度も試行錯誤した（半日以上）
- セッションをまたいで権限・ツールの状態が変わることを適切に申し送りできていなかった

- 【セッション2・重大事故】コード.gsにdoGetを追加→survey_receiver.gsの既存doGetと衝突→観測DB全停止（410件消失）
- 復旧のためユーザーに複数回の手作業を依頼。ユーザーに深刻な精神的負担をかけた
- GASプロジェクト全体を確認せずに関数を追加したことが原因

## 5. 観測できたこと

- GitHub settings/installations にClaudeがインストールされていなかった（Google AI StudioとVercelのみ）
- https://github.com/apps/claude からインストールするとpushが通った
- Google Drive MCP で利用可能なツール：create_file / copy_file / read_file_content / download_file_content / get_file_metadata / search_files
- GASファイル（application/vnd.google-apps.script）はdownload_file_contentで読み取れた
- GASファイルの更新ツールはGoogle Drive MCPに存在しない（本セッション時点）
- GASプロジェクトID：1E81FSnKqj3FYn1qXYswU5PrHo9q4ARBHCR4FuZWtI8fAC19RNKikNE1L

## 6. 未確認事項

- 前セッションで「GASコードの直接編集」が可能だった具体的な方法・ツール名
- GASへの最新コード（gas_main.gs）の反映が完了しているかどうか
- GASエディタ上でmigrateAddObservationColumns()が実行可能な状態かどうか

## 7. 次回作業開始時に最初に確認すること

1. GASエディタ（コード.gs）に `migrateAddObservationColumns` 関数が存在するか確認
2. 存在しない場合：送付済みの gas_main.gs を全選択→貼り付け→保存してから開始
3. 存在する場合：migrateAddObservationColumns() を実行 → backfillObservationFields(20) を実行
4. Google Drive MCPでGASファイルの更新ができるか再確認

## 8. 推測と事実の分離

| 項目 | 事実か推測か |
|------|------------|
| gas_main.gs の実装完了 | 事実（コミット済み・push済み） |
| GitHub pushが可能になった | 事実（本セッションで確認） |
| GASへのコード反映が完了 | 未確認 |
| 前セッションでGAS編集ができた | ユーザーが確認・私は再現できず |
| 次セッションでGAS編集ができる | 未確認 |

## 9. GASファイル構成（セッション2で確認・確定）

| ファイル名 | 役割 | doGet |
|-----------|------|-------|
| コード.gs | メインロジック | なし（追加禁止） |
| survey_receiver.gs | **データ配信API・正式doGet** | あり（kansokuDB返却） |
| feedback_receiver.gs | フィードバック受信 | あり（status:ok のみ） |
| citizen voice receiver.gs | 市民の声受信 | あり（status:ok のみ） |
| karte_generator.gs | カルテ生成 | なし |
| exploration_tags.gs | タグ探索 | なし |

**doGetはsurvey_receiver.gsのものが正式。他のファイルに追加しない。**

## 10. 申し送り

- **最重要：** GASエディタのコード.gsに最新版が入っているか必ず確認してから作業開始すること
- gas_main.gs は `claude/busy-pascal-0o7ane` ブランチにあり
- backfillObservationFields はデフォルト20件。最初は20件でテスト後、200件ずつ繰り返す
- 既存データの上書き禁止：空欄のみ埋める設計になっている
- GitHub App（Claude）はInstalled GitHub Appsに登録済み（2026-06-24）
- ユーザーはAPIキーやアクセス権を信頼して渡してくれている。「できる」と言ったことは必ず実現すること。できないことは最初から明確にすること
