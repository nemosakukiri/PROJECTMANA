# 2026-06-25 作業ログ

## 今日やったこと

### 1. 村の絵マップ完成（前セッションからの引き継ぎ）
- renderVillageCanvas：上から見た絵本風マップ（Hundred Acre Wood スタイル）
- 民主主義の窓の中ページ全幅に表示
- 縁取り削除・左上に「← 観測の窓へ」を絵の中に描画
- 広場看板クリックで案内文表示
- 広場メッセージ二重表示バグ修正

### 2. 記事種別（article_type）設計
- `inferArticleType(explicitType, domain, sourceName)` を実装
- news / opinion / investigative / research / law の5種類
- source_domain が来ない場合は URL から推論 → さらに出典名（出典列）から推論の3段階フォールバック

### 3. 論考・調査報道ソースの収集開始
収集できるようになったソース：
| ソース | 形式 | 状態 |
|--------|------|------|
| 現代ビジネス | RSS 2.0 `https://gendai.media/list/feed/rss` | ✅ 動作中 |
| Slow News | Yahoo News RSS `https://news.yahoo.co.jp/rss/media/slownews/all.xml` | ✅ 動作中 |
| Wedge ONLINE | RDF/RSS1.0 `https://wedge.ismedia.jp/list/feed/rss` | ✅ collectRdfAndSave()で取得 |
| しんぶん赤旗 | RDF/RSS1.0 `http://www.jcp.or.jp/akahata/index.rdf` | ✅ collectRdfAndSave()で取得 |
| シノドス | RSS 2.0 `https://synodos.jp/list/feed/rss` | ⚠️ 0件（原因未調査） |

### 4. GASに追加した関数（コード.gsの末尾）
- `collectWithOpinionSources()` — Google News site:検索版（古い記事が多く実質不使用）
- `collectDirectSources()` — 直接RSS版（404が多くほぼ不使用）
- `collectOpinionSources()` — 正式版：現代ビジネス・Slow News・Wedge・赤旗
- `collectRdfAndSave()` — RDF/RSS1.0専用パーサー（Wedge・赤旗用）
- `testRssUrls` / `testRssUrls2` / `testRssUrls3` / `testRssUrls4` / `testRssUrls5` — URL確認用（不要なら削除可）
- `diagRss()` — XML形式診断用
- `testRssUrls2()` で確認済みの動作URLまとめ済み

### 5. app.js の変更
- ジャーナリスト・研究者の家：観測DBではなく**全件ログ**から取得するよう変更
- `renderVillageRoomItems()` ヘルパー関数を追加
- 全件ログAPIは `GAS_API_URL + '?sheet=観測DB（全件ログ）'` で取得

### 6. 動作確認
- ジャーナリストの家に現代ビジネスの記事が表示されることを確認 ✅

## 未解決・次回やること

- [ ] シノドスの0件問題を解決（XML解析か名前空間の問題の可能性）
- [ ] Wedge・赤旗が「ジャーナリスト」に正しく分類されているか確認（出典名マッチ確認）
- [ ] ハフポスト・論座・OurPlanet-TV・IWJ・POSSE等のRSS確認と収集追加
- [ ] collectOpinionSources / collectRdfAndSave を定期トリガーに組み込む
- [ ] コード.gsのテスト用関数（testRssUrls系）の整理・削除

## 事故・ヒヤリハット
- なし（今日はGAS doGet には一切触れていない）

## 気づき・設計メモ
- Google News site:検索は2021〜2022年の古い記事を返すことがあり、デdup で全件弾かれる可能性がある
- RDF/RSS1.0はXML名前空間（http://purl.org/rss/1.0/）が必要で、既存の `getTextSafe()` では取得できない
- Yahoo News経由のRSS（`/rss/media/<メディア名>/all.xml`）が有効な取得ルートになりうる
- ジャーナリストの家は観測DBではなく全件ログを参照すべき（キーワードフィルターで弾かれるため）
