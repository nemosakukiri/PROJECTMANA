# WORKLOG 2026-07-13

## 今日やったこと：観測DB → X自動投稿の実装

### 背景
MANAをXで紹介する方法として、「観測DBから1日1件、自動でXに投稿する」定点観測アカウント運用を採用。

### やったこと

1. **X Developer Consoleのセットアップ**
   - X APIプロジェクト（Pay-Per-Use）作成
   - auのカードで$5チャージ（無期限クレジット、リンクなし運用なら約1年分）
   - $500無料クレジット自動付与は今回は付かなかった
   - アプリ `mana-x-bot` 作成、権限を「Read and Write」に設定
   - Consumer Keys・Access Token/Secretを発行（途中誤ってチャットに貼りそうになった鍵は都度再生成して無効化済み）

2. **x_poster.gs の実装（新規ファイル、doGetには一切触れず）**
   - `コード.gs` の全101関数を事前確認し、名前衝突なしを確認してから追加（CLAUDE.mdのGAS変更ルール遵守）
   - kansokuDB（公開観測DB）から未投稿記事を1件選ぶ `pickUnpostedObservation()`
   - 投稿済みURLを記録する専用シート「X投稿履歴」で重複投稿を防止（X利用規約の「同一内容の連投禁止」対策）
   - 280文字に収まるよう投稿文を自動整形 `buildTweetText()`
   - OAuth 1.0a署名を自前実装（GASにOAuthライブラリが無いため） `buildOAuth1Header()`
   - 鍵はスクリプトプロパティに保存（`X_API_KEY` / `X_API_SECRET` / `X_ACCESS_TOKEN` / `X_ACCESS_TOKEN_SECRET`）。既存の `setupApiKey()` 等と同じ命名パターンに合わせた
   - 独立トリガー `setXPostTrigger()`：毎日8:00に `postDailyObservationToX()` を実行。既存の `setDailyTrigger()` は変更していない

3. **GitHub ⇄ GAS 自動同期の発見**
   - このGASプロジェクトはGitHubリポジトリ（main）と同期する設定になっていた
   - ただし完全自動ではなく、GAS側で🔽（プル）ボタンを押す操作が都度必要

### 401エラーの原因切り分け（正しく実装できていたことの確認）

投稿・読み取り共に401 Unauthorizedが発生。以下の手順で原因を切り分けた：

1. `diagnoseXKeyFormat()` — 鍵の長さ・空白・ハイフン位置を確認 → **問題なし**（API Key 25文字、API Secret 50文字、Access Token 50文字＋ハイフンあり、Access Token Secret 45文字、いずれも空白なし）
2. `verifyXCredentials()` — 投稿を伴わない読み取り専用API（GET /2/users/me）でテスト → **これも401**（書き込み権限固有の問題ではないと判明）
3. `testOAuthSignatureAlgorithm()` — Twitter公式ドキュメントの既知サンプル値で署名計算を検証 → **完全一致、アルゴリズムは正しい**
4. 鍵は「Consumer Keys」「Access Token and Secret」を同時に再生成して組み合わせのズレも排除済み

### 結論：X社側の既知の不具合と判明

X Developer Community公式フォーラムに同一症状の複数報告あり：
- [Pay-Per-Use app: OAuth 1.0a user context returns 401 Unauthorized on POST /2/tweets — persists across new app and regenerated keys](https://devcommunity.x.com/t/pay-per-use-app-oauth-1-0a-user-context-returns-401-unauthorized-on-post-2-tweets-persists-across-new-app-and-regenerated-keys/270441)
- [Pay Per Use app returns 401 on all OAuth 1.0a auth attempts](https://devcommunity.x.com/t/pay-per-use-app-returns-401-on-all-oauth-1-0a-auth-attempts-code-89-then-code-32-after-recreating-app/270372)
- [OAuth 1.0a User Context returns 401 on all v2 endpoints (Bearer works) — likely backend enrollment issue](https://devcommunity.x.com/t/oauth-1-0a-user-context-returns-401-on-all-v2-endpoints-bearer-works-pay-per-use-app-likely-backend-enrollment-issue-app-id-33004531/267100)

> 2026年にPay-Per-Use（従量課金）で新規作成したアプリは、X社バックエンド側でOAuth 1.0aユーザーコンテキストの認証登録が正しく行われないことがある既知の不具合。アプリを作り直しても、鍵を再生成しても直らないケースが報告されている。こちら側の実装・設定のミスではない。

---

## 追記（翌朝・2026-07-14未明のセッション）：完全解決・稼働開始

### 解決までの流れ

1. 翌朝、`verifyXCredentials()` を再実行 → まだ401（自然解消は無し）
2. `testPostDailyObservationToX()` も再実行 → まだ401（症状変わらず）
3. フォーラムで報告されていたもう一つの対処法「アプリを新規に作り直す」を実施
   - 新しいアプリ（別名）を作成し、権限を最初から「Read and Write」に設定
   - 新しく発行された4鍵にスクリプトプロパティを丸ごと入れ替え
4. `verifyXCredentials()` → **200 成功**（`{"data":{"id":"1095377114","name":"イイカンジ(MANA.T)","username":"currynoodler"}}`）
5. `testPostDailyObservationToX()` → **投稿成功**（観測DBの記事を実際に1件投稿）
6. `setXPostTrigger()` → **トリガー設定完了**（毎日8:00ごろ自動実行）

### 結論

原因は前日の記録通りX社側のバックエンド不具合で、**そのアプリ単体が「登録不良」状態になっていた**ことが確定した。新規アプリの作成で回避できた。時間経過による自然解消は今回は起きなかった。

### 現在の状態：稼働中

- `@currynoodler`（イイカンジ(MANA.T)）アカウントで、**毎日8:00ごろ、観測DBから未投稿の記事が自動で1件投稿される**状態が完成。
- 投稿ログは「X投稿履歴」シートに自動記録される。
- 重複投稿防止・280文字整形・老朽記事の除外は実装済み。

## 次のセッションでやること

- しばらく運用を見守り、投稿内容・頻度・反応を確認する
- 必要であれば投稿文のトーンやハッシュタグ運用を調整する

## 事故・失敗

- なし。ただしアクセストークンをチャットに誤って貼りそうになった場面が複数回あり、都度その場で再生成して無効化した。今後も「鍵の文字列そのものは絶対にチャットに貼らない」を徹底する。

## メモ・気づき

- Xの支払いはチャージ式（前払い・無期限）。デビットカード（au）でも問題なく利用できた。
- 無料クレジット$500は今回は付与されなかった。
- X APIの「Public Utility（無料アクセス）」枠は政府機関の防災・交通情報等に限定されており、MANAのような市民プロジェクトは対象外の可能性が高い。
- Pay-Per-Useアプリの「バックエンド登録不良」問題は、時間待ちより「アプリを作り直す」方が確実な対処法だった。
